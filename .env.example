# Telegram contact form credentials (server-side only)
# NEVER prefix with NEXT_PUBLIC_ — these must never reach the browser.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Download link shown on the About page
CV_URL=